{% extends 'base.html.twig' %}

{% block title %}Edit Book{% endblock %}

{% block body %}
<div class="container containerForm">
        <div class="form">
            <h1>Modifier la fiche d'un livre</h1>
                
            
            
            {{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('app_book_index') }}"  class="btn btn-primary">Revenir à la liste</a>
            <a href="{{ path('app_stock_add_history', {'id': book.id}) }}" class="btn btn-primary">Voir l'historique</a>
            {{ include('book/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
