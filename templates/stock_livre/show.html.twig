{% extends 'base.html.twig' %}

{% block title %}StockLivre{% endblock %}

{% block body %}
    <h1>StockLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock_livre.id }}</td>
            </tr>
            <tr>
                <th>Livre</th>
                <td>{{ stock_livre.Livre }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ stock_livre.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_livre_index') }}">back to list</a>

    <a href="{{ path('app_stock_livre_edit', {'id': stock_livre.id}) }}">edit</a>

    {{ include('stock_livre/_delete_form.html.twig') }}
{% endblock %}
